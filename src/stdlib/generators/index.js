import filter from "./filter";
import input from "./input";
import map from "./map";
import range from "./range";
import valueAt from "./valueAt";

export default {
  filter: filter,
  input: input,
  map: map,
  range: range,
  valueAt: valueAt
};
