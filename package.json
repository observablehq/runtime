{
  "name": "@observablehq/notebook-runtime",
  "version": "0.0.0",
  "description": "The Observable notebook runtime.",
  "author": {
    "name": "Observable, Inc."
  },
  "license": "ISC",
  "main": "build/notebook-runtime.js",
  "module": "src/index",
  "repository": {
    "type": "git",
    "url": "https://github.com/@observablehq/notebook-runtime.git"
  },
  "scripts": {
    "pretest": "rm -rf build && mkdir build && rollup -c --banner \"$(preamble)\" -f umd -n O -o build/notebook-runtime.js -- src/index.js",
    "test": "tape 'test/**/*-test.js' && eslint index.js src",
    "prepublish": "npm run test && uglifyjs --preamble \"$(preamble)\" build/notebook-runtime.js -c -m -o build/notebook-runtime.min.js",
    "postpublish": "git push && git push --tags && zip -j build/notebook-runtime.zip -- LICENSE README.md build/notebook-runtime.js build/notebook-runtime.min.js"
  },
  "devDependencies": {
    "d3-require": "^0.4.0",
    "d3-time-format": "^2.0.5",
    "eslint": "^3.19.0",
    "package-preamble": "0.1.0",
    "rollup": "^0.41.6",
    "rollup-plugin-node-resolve": "^3.0.0",
    "tape": "^4.6.3",
    "uglify-js": "git+https://github.com/mishoo/UglifyJS2.git#harmony-v2.8.22"
  }
}
